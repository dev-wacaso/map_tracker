spring:
  application:
    name: map-tracker
  datasource:
    url: jdbc:postgresql://localhost:5432/map_tracker
    username: tracker
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  data:
    redis:
      host: localhost
      port: 6379

  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 0
  jackson:
    property-naming-strategy: SNAKE_CASE

logging:
  level:
    org.flywaydb: INFO
    org.springframework.jdbc.datasource: INFO
    org.postgresql: INFO
    org.hibernate.SQL: INFO
    com.zaxxer.hikari: INFO
    org.hibernate.type.descriptor.sql.BasicBinder: INFO

server:
  port: 8000

app:
  config:
    transmitter-interval-seconds: 300
    zoom-threshold-heatmap: 7.0
    zoom-threshold-detail: 11.0
    h3-resolution-heatmap: 5
    h3-resolution-detail: 8
    viewer-refresh-interval-default-seconds: 300
